package JAVA_CORE;

public class cicle_while {
    public static void main(String[] args) {
        int count = 0;

        while (count < 10) {                       // не знаем какое количество итераций необходимо выполнить,
            System.out.println(count);              // если знаем количество итераций то используем цикл for
            count++;
            if ((count % 2) == 0);

        }
    }
}
